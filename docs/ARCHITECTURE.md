# AgentSannyii 系统架构设计

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        AgentSannyii Platform                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │   首页展示    │───▶│  Utility展示  │───▶│  Utility运行  │      │
│  │  (已构建)     │    │  (/tool/:id) │    │  (内嵌/下载)  │      │
│  └──────────────┘    └──────────────┘    └──────────────┘      │
│         │                   │                                      │
│         ▼                   ▼                                      │
│  ┌──────────────────────────────────┐                          │
│  │         生成器界面 (/create)       │                          │
│  │  ┌─────────┐ ┌─────────┐ ┌────┐  │                          │
│  │  │Prompt输入│ │代码预览 │ │下载│  │                          │
│  │  └─────────┘ └─────────┘ └────┘  │                          │
│  └──────────────────────────────────┘                          │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│                         数据存储层                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐    ┌──────────────────┐                  │
│  │   公共工具库      │    │   用户私有工具库   │                  │
│  │  (代码库管理)     │    │  (数据库存储)     │                  │
│  ├──────────────────┤    ├──────────────────┤                  │
│  │ • 官方维护       │    │ • 用户创建        │                  │
│  │ • 版本控制       │    │ • 可分享/私密     │                  │
│  │ • 质量审核       │    │ • 云端同步        │                  │
│  │ • 分类标签       │    │ • 可导出HTML      │                  │
│  └──────────────────┘    └──────────────────┘                  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📦 存储方案设计

### 1. 公共工具库（官方）

**存储位置**: `public/tools/` 

**文件格式**:
```
public/tools/
├── image-slicer.html      # 独立HTML文件
├── gif-splitter.html
├── format-converter.html
└── meta.json              # 工具元数据索引
```

**meta.json 结构**:
```json
{
  "tools": [
    {
      "id": "image-slicer",
      "title": "Image Slice Lab",
      "description": "Cut sprites or social images into precise tiles",
      "category": "Image",
      "version": "1.0.0",
      "author": "AgentSannyii Team",
      "runtime": "offline",
      "tags": ["image", "sprite", "grid"],
      "createdAt": "2025-01-15",
      "updatedAt": "2025-02-01",
      "file": "image-slicer.html"
    }
  ]
}
```

### 2. 用户私有工具库

**方案A: 浏览器本地存储**（MVP阶段）
- 使用 IndexedDB 
- 支持导出为HTML文件

**方案B: 云端数据库**（扩展阶段）
- Supabase / Firebase
- 用户认证系统

**数据表结构**:
```typescript
interface UserTool {
  id: string;
  userId: string;
  title: string;
  description: string;
  category: string;
  htmlContent: string;
  isPublic: boolean;
  shareId?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 🎨 单HTML Utility 规范

### 模板结构

每个工具必须是**完全自包含的单个HTML文件**，包含：

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{ToolName} | AgentSannyii Utility</title>
  
  <!-- 极光主题CSS变量 - 与首页一致 -->
  <style>
    :root {
      --void: #050508;
      --void-lighter: #0a0a12;
      --aurora-cyan: #00f5ff;
      --aurora-pink: #ff2a9d;
      --aurora-purple: #bd34fe;
      --text-primary: #ffffff;
      --text-tertiary: rgba(255, 255, 255, 0.55);
    }
  </style>
</head>
<body>
  <div id="app"></div>
  <script>
    // 工具逻辑代码
  </script>
</body>
</html>
```

---

## 🛠️ 生成器界面设计

### 布局结构

```
┌────────────────────────────────────────────────────────────────┐
│  AgentSannyii Utility Generator                    [关闭] [?]   │
├──────────────────────────┬─────────────────────────────────────┤
│                          │                                     │
│  ┌────────────────────┐  │  ┌─────────────────────────────┐   │
│  │ 🤖 Describe your   │  │  │                             │   │
│  │    utility...      │  │  │      [Live Preview]         │   │
│  │                    │  │  │                             │   │
│  │ "A tool that..."   │  │  │    ┌─────────────┐          │   │
│  │                    │  │  │    │   UI Mock   │          │   │
│  │ [Advanced ▼]       │  │  │    └─────────────┘          │   │
│  └────────────────────┘  │  │                             │   │
│                          │  └─────────────────────────────┘   │
│  [⚡ Generate Utility]   │                                     │
│                          │  ┌─────────────────────────────┐   │
│  Generation Options:     │  │  [HTML] [CSS] [JS] [Preview]│   │
│  ○ Quick (GPT-4o)        │  └─────────────────────────────┘   │
│  ● Quality (Claude)      │                                     │
│                          │  [💾 Save] [📥 Download] [🔗 Share]│
└──────────────────────────┴─────────────────────────────────────┘
```

---

## 🚀 实施路线图

### Phase 1: MVP（当前）
- [x] 首页UI重构
- [ ] 创建基础HTML模板
- [ ] 实现2-3个示例工具
- [ ] 本地存储用户工具

### Phase 2: 生成器
- [ ] AI集成（OpenAI/Anthropic）
- [ ] 生成器UI界面
- [ ] 实时预览功能

### Phase 3: 生态
- [ ] 用户认证系统
- [ ] 云端数据库存储
- [ ] 工具分享市场

---

*设计文档 v1.0*
