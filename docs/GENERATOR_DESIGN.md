# Utility Generator 生成器界面设计

## 🎯 设计目标

创建一个类似 V0.dev / Claude Artifacts 的界面，让用户通过自然语言描述即可生成自定义工具。

---

## 📐 布局结构

### 桌面端 - 三栏布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 🤖 AgentSannyii Generator                                      [Close] [?]   │
├────────────────────────┬────────────────────────┬───────────────────────────┤
│                        │                        │                           │
│   INPUT PANEL          │   PREVIEW PANEL        │   CODE PANEL              │
│   (Left Sidebar)       │   (Center)             │   (Right Sidebar)         │
│                        │                        │                           │
│  ┌──────────────────┐  │  ┌──────────────────┐  │  ┌─────────────────────┐  │
│  │ 📝 Describe...   │  │  │                  │  │  │ [HTML][CSS][JS]     │  │
│  │                  │  │  │    LIVE          │  │  │                     │  │
│  │ "Create a tool   │  │  │    PREVIEW       │  │  │  <html>             │  │
│  │  that converts   │  │  │                  │  │  │    <head>...        │  │
│  │  JSON to CSV..." │  │  │  ┌────────────┐  │  │  │    <body>           │  │
│  │                  │  │  │  │            │  │  │  │      ...            │  │
│  │                  │  │  │  │   Tool UI  │  │  │  │    </body>          │  │
│  │ [📎 Upload]      │  │  │  │            │  │  │  │  </html>            │  │
│  └──────────────────┘  │  │  └────────────┘  │  │  │                     │  │
│                        │  │                  │  │  └─────────────────────┘  │
│  ─────────────────────  │  │  [🖥️][📱]       │  │                           │
│                        │  │  Desktop/Mobile  │  │  [📋 Copy] [💾 Save]      │
│  ⚙️ OPTIONS             │  │                  │  │                           │
│  ┌──────────────────┐  │  └──────────────────┘  │                           │
│  │ ○ Quick (GPT)    │  │                        │                           │
│  │ ● Quality (Claude)│  │                        │                           │
│  │                  │  │                        │                           │
│  │ [✨ Generate]    │  │                        │                           │
│  └──────────────────┘  │                        │                           │
│                        │                        │                           │
│  ─────────────────────  │                        │                           │
│                        │                        │                           │
│  📋 TEMPLATES           │                        │                           │
│  ┌──────────────────┐  │                        │                           │
│  │ 🧹 Text Cleaner  │  │                        │                           │
│  │ 🎨 Image Cropper │  │                        │                           │
│  │ 📊 Data Filter   │  │                        │                           │
│  └──────────────────┘  │                        │                           │
│                        │                        │                           │
└────────────────────────┴────────────────────────┴───────────────────────────┘
```

### 移动端 - 标签页切换

```
┌─────────────────────────────────────┐
│ 🤖 Generator              [Close]   │
├──────────┬──────────┬───────────────┤
│  Input   │ Preview  │     Code      │
├──────────┴──────────┴───────────────┤
│                                     │
│   [当前选中的面板内容]                │
│                                     │
│   ┌─────────────────────────────┐   │
│   │                             │   │
│   │    根据选择的标签页显示       │   │
│   │    不同的面板内容            │   │
│   │                             │   │
│   └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

---

## 🎨 组件详细设计

### 1. 输入面板 (Input Panel)

#### 自然语言输入框
```
┌──────────────────────────────────┐
│ 📝 What do you want to build?    │
├──────────────────────────────────┤
│                                  │
│  Create a tool that...           │
│                                  │
│  • Converts between formats      │
│  • Processes images              │
│  • Generates codes/IDs           │
│  • Transforms data               │
│                                  │
│  [Type / for suggestions...]     │
│                                  │
└──────────────────────────────────┘
        [📎 Attach files]
```

**功能：**
- 大文本输入区，支持多行
- 拖拽文件到文本区上传
- `/` 触发命令菜单（类似 Notion）
- 实时字数统计

#### 参考文件上传
```
┌──────────────────────────────────┐
│ 📎 Reference Files               │
├──────────────────────────────────┤
│                                  │
│  [Drop files here]               │
│  or click to browse              │
│                                  │
│  ┌────────┐ ┌────────┐          │
│  │ 📄 doc │ │ 🖼️ img │          │
│  │  ✕     │ │  ✕     │          │
│  └────────┘ └────────┘          │
│                                  │
└──────────────────────────────────┘
```

**支持格式：**
- 图片（UI 设计稿、草图）
- 文档（需求说明）
- JSON/XML（示例数据）

#### 生成选项
```
┌──────────────────────────────────┐
│ ⚙️ Generation Options            │
├──────────────────────────────────┤
│                                  │
│  Model:                          │
│  ○ GPT-4o Mini (Fast)            │
│  ● GPT-4o (Balanced)             │
│  ○ Claude 3.5 (Quality)          │
│                                  │
│  Theme:                          │
│  ● Aurora (Default)              │
│  ○ Minimal                       │
│  ○ Custom...                     │
│                                  │
│  [✨ Generate Utility]           │
│                                  │
└──────────────────────────────────┘
```

#### 模板库
```
┌──────────────────────────────────┐
│ 📋 Quick Templates               │
├──────────────────────────────────┤
│                                  │
│  🧹 Text Formatter          →    │
│  Format, clean, transform text   │
│                                  │
│  🎨 Image Resizer           →    │
│  Resize, crop, compress images   │
│                                  │
│  📊 CSV Processor           →    │
│  Sort, filter, transform CSV     │
│                                  │
│  🔗 URL Shortener           →    │
│  Generate short links            │
│                                  │
│  [Browse All Templates...]       │
│                                  │
└──────────────────────────────────┘
```

### 2. 预览面板 (Preview Panel)

#### 工具预览
```
┌──────────────────────────────────┐
│  🖥️ Preview          [🔄 Refresh] │
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐  │
│  │                            │  │
│  │     ┌────────────────┐     │  │
│  │     │                │     │  │
│  │     │   Generated    │     │  │
│  │     │   Tool UI      │     │  │
│  │     │                │     │  │
│  │     │  (Interactive) │     │  │
│  │     │                │     │  │
│  │     └────────────────┘     │  │
│  │                            │  │
│  └────────────────────────────┘  │
│                                  │
│  [🖥️ Desktop] [📱 Mobile]        │
│                                  │
└──────────────────────────────────┘
```

**功能：**
- 内嵌 iframe 隔离运行
- 桌面/移动端尺寸切换
- 刷新重新加载
- 全屏预览按钮

#### 迭代对话
```
┌──────────────────────────────────┐
│ 💬 Refine                        │
├──────────────────────────────────┤
│                                  │
│  You: Make the button larger     │
│                                  │
│  🤖 Done! I've increased the     │
│     button size and improved     │
│     the contrast.                │
│                                  │
│  ┌────────────────────────────┐  │
│  │ Make it blue instead...    │  │
│  └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

### 3. 代码面板 (Code Panel)

#### 代码查看器
```
┌──────────────────────────────────┐
│  [HTML] [CSS] [JS] [📋 Copy All] │
├──────────────────────────────────┤
│                                  │
│  1  │ <!DOCTYPE html>           │
│  2  │ <html lang="en">          │
│  3  │ <head>                    │
│  4  │   <meta charset="UTF-8">  │
│  5  │   <style>                 │
│  6  │     /* Aurora Theme */    │
│  7  │     :root {               │
│  8  │       --void: #050508;    │
│  9  │     }                     │
│ 10  │   </style>                │
│ ... │ ...                       │
│     │                           │
│     │                           │
└──────────────────────────────────┘
```

**功能：**
- Monaco Editor 语法高亮
- HTML/CSS/JS 标签切换
- 行号显示
- 代码折叠
- 搜索/替换

#### 操作按钮
```
┌──────────────────────────────────┐
│                                  │
│  [💾 Save to My Tools]           │
│                                  │
│  [📥 Download HTML]              │
│                                  │
│  [🔗 Share (Public)]             │
│                                  │
│  [🗑️ Delete]                     │
│                                  │
└──────────────────────────────────┘
```

---

## 🔄 用户流程

### 流程 1：从头创建

```
1. 用户进入 /create 页面
        │
        ▼
2. 输入自然语言描述
   "创建一个将JSON转为CSV的工具"
        │
        ▼
3. 可选：上传参考文件
        │
        ▼
4. 点击 [✨ Generate]
        │
        ▼
5. 显示加载动画
   "🤖 AI is crafting your utility..."
        │
        ▼
6. 生成完成，三栏显示
   - 左侧：可继续对话修改
   - 中间：实时预览
   - 右侧：查看代码
        │
        ▼
7. 用户测试满意后
        │
        ├─▶ [💾 Save] → 保存到"我的工具"
        │
        ├─▶ [📥 Download] → 下载HTML文件
        │
        └─▶ [🔗 Share] → 生成分享链接
```

### 流程 2：从模板创建

```
1. 用户点击模板 "🧹 Text Cleaner"
        │
        ▼
2. 系统预填充描述
   "Create a text cleaning utility
    that removes extra spaces..."
        │
        ▼
3. 用户可修改描述
        │
        ▼
4. [✨ Generate] 基于模板生成
        │
        ▼
5. 继续流程同流程1
```

### 流程 3：迭代优化

```
当前版本: v1 (基础功能)
        │
        ▼
用户: "添加一个复制按钮"
        │
        ▼
AI 修改代码
        │
        ▼
新版本: v2 (添加复制功能)
        │
        ▼
用户可:
  ├─▶ 继续修改 (v3, v4...)
  ├─▶ 回滚到 v1
  └─▶ 对比 v1 vs v2
```

---

## 🛠️ 技术实现建议

### 前端架构

```typescript
// 页面结构
export default function CreatePage() {
  return (
    <GeneratorLayout>
      <InputPanel />
      <PreviewPanel />
      <CodePanel />
    </GeneratorLayout>
  );
}

// 状态管理
interface GeneratorState {
  // 输入
  prompt: string;
  attachedFiles: File[];
  
  // 生成中
  isGenerating: boolean;
  generationProgress: number;
  
  // 输出
  generatedHtml: string;
  currentVersion: number;
  versions: Version[];
  
  // UI
  activeTab: 'preview' | 'html' | 'css' | 'js';
  previewMode: 'desktop' | 'mobile';
}
```

### AI 集成

```typescript
// Prompt 工程
const SYSTEM_PROMPT = `
You are a utility generator for AgentSannyii.
Create a single-file HTML tool based on the user's request.

Requirements:
1. Must be a single, self-contained HTML file
2. Use the Aurora Dream theme CSS variables
3. Include all CSS inline in <style>
4. Include all JS inline in <script>
5. No external dependencies except data URIs for images
6. Must work offline
7. Responsive design
8. Include proper error handling

Theme Variables:
- --void: #050508 (background)
- --aurora-cyan: #00f5ff (accent)
- --aurora-purple: #bd34fe (accent)
- --aurora-pink: #ff2a9d (accent)

Provide ONLY the complete HTML code, no explanations.
`;

// API 调用
async function generateUtility(prompt: string, files?: File[]) {
  const response = await fetch('/api/generate', {
    method: 'POST',
    body: JSON.stringify({ prompt, files }),
  });
  
  const { html } = await response.json();
  return html;
}
```

### 沙箱预览

```typescript
// 安全的 iframe 预览
function PreviewPanel({ html }: { html: string }) {
  const iframeRef = useRef<HTMLIFrameElement>(null);
  
  useEffect(() => {
    if (iframeRef.current) {
      const doc = iframeRef.current.contentDocument;
      doc.open();
      doc.write(html);
      doc.close();
    }
  }, [html]);
  
  return (
    <iframe
      ref={iframeRef}
      sandbox="allow-scripts allow-same-origin"
      style={{ width: '100%', height: '100%', border: 'none' }}
    />
  );
}
```

---

## 📱 响应式断点

| 断点 | 布局 | 说明 |
|-----|------|------|
| ≥1400px | 三栏 | 完整桌面体验 |
| 1024-1399px | 两栏 | 代码面板可折叠 |
| 768-1023px | 单栏+标签 | 标签切换面板 |
| <768px | 单栏+标签 | 简化移动端 |

---

## 🎨 动效规范

| 元素 | 动画 | 时长 | Easing |
|-----|------|------|--------|
| 面板滑入 | translateX | 400ms | ease-out |
| 生成中脉冲 | scale + opacity | 1500ms | ease-in-out |
| 代码高亮 | background | 300ms | ease |
| 预览刷新 | fade | 200ms | linear |
| 消息滑入 | translateY | 300ms | ease-out |

---

*设计文档 v1.0 - 2025.02*
